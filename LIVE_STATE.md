# LIVE_STATE.md — Sovereign Mission Control

> **⚡ AUTO-GENERATED.** Do not edit by hand. Run `npm run verify-state` to regenerate.
> This file is the single source of truth for current runtime state. If
> `MISSION-CONTROL-MASTER-REFERENCE.md` contradicts this file, **this file wins** —
> the master reference holds invariants and history, not live values.

**Last verified:** `2026-04-10T21:22:00Z` (hand-generated first-gen — run `npm run verify-state` to refresh)
**Generator:** `scripts/verify-state.mjs`

## Git State
Run `npm run verify-state` from the repo root to populate git SHA, branch, and dirty status. First-gen placeholder.

## Package
- **Name:** `mission-control-dashboard`
- **Version:** `0.1.0`
- **Next.js:** `16.2.0`
- **React:** `19.2.4`
- **@supabase/supabase-js:** `^2.99.3`

## App Router Surface — src/app (verbatim filesystem walk)
**15 page(s)** (first-gen snapshot — refresh via script for accuracy)

### Pages
- `/` → `src/app/page.tsx`
- `/brain` → `src/app/brain/page.tsx`
- `/connections` → `src/app/connections/page.tsx`
- `/content` → `src/app/content/page.tsx`
- `/crew` → `src/app/crew/page.tsx`
- `/crew/[agent]` → `src/app/crew/[agent]/page.tsx`
- `/finance` → `src/app/finance/page.tsx`
- `/glitch` → `src/app/glitch/page.tsx`
- `/onboarding` → `src/app/onboarding/page.tsx`
- `/portals` → `src/app/portals/page.tsx`
- `/productivity` → `src/app/productivity/page.tsx`
- `/products` → `src/app/products/page.tsx`
- `/settings` → `src/app/settings/page.tsx`
- `/tasks` → `src/app/tasks/page.tsx`
- `/vid-rush` → `src/app/vid-rush/page.tsx`

## Supabase Client — src/lib/supabase.ts
### Environment
Presence status populated when script runs. First-gen placeholder — refresh via `npm run verify-state`.

### Source (verbatim)
```typescript
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL || 'https://placeholder.supabase.co';
const supabaseKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || 'placeholder';

export const supabase = createClient(supabaseUrl, supabaseKey);
```

## Critical Environment Variables (presence only)
> Only SET / UNSET status is shown. Secret values are never printed.
> First-gen placeholder — run `npm run verify-state` to populate live status.

**Supabase (client)**
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

**Supabase (server)**
- `SUPABASE_SERVICE_ROLE_KEY`

**Resend (nurture emails)**
- `RESEND_API_KEY`

**Stripe (webhooks)**
- `STRIPE_SECRET_KEY`
- `STRIPE_WEBHOOK_SECRET`

**Bot ↔ MC bridge**
- `MC_WEBHOOK_URL`
- `WEBHOOK_SHARED_SECRET`

---

## Session-Start Cross-Check Protocol

Every session touching Mission Control must run this check before trusting any
"current state" claim in the master reference:

1. Read `MISSION-CONTROL-MASTER-REFERENCE.md` (invariants + history)
2. Read this file (`LIVE_STATE.md`)
3. If this file is older than 24h → run `npm run verify-state` first
4. If the master reference contradicts this file → **this file wins**
5. Flag the contradiction and patch the master reference before continuing work

This protocol exists because session-authored references rot, and code does not.

### Sentinel Bot cross-check
For TTS routing, LLM teams, and Railway env vars, the authoritative source is
`C:\Users\richi\Sovereign-Sentinel-Bot\LIVE_STATE.md` (generated by the Sentinel Bot's
own `scripts/verify-state.ts`). Do not infer Sentinel Bot runtime state from this file.
